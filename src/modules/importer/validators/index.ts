// Placeholder para validações de campos da engine de importação.

export const validatePartRow = (_row: Record<string, unknown>) => {
  return true;
};

export { validatePart } from './validatePart';
